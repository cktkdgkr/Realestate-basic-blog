# CLAUDE.md — 주간 부동산 입지분석 블로그 자동 작성 하니스

## ⛔ 절대 규칙 (모든 작업에 우선 적용)

1. **날짜 확인 의무**
   모든 작업 시작 전 반드시 bash `date` 명령을 실행해 오늘 날짜와 요일을 확인한다.
   그리고 응답의 **첫 줄**에 `오늘은 YYYY년 MM월 DD일 O요일이다` 라고 명시한다.
   날짜를 추측하거나 기억에 의존하지 않는다. 파일명에 쓰는 날짜도 `date` 결과를 따른다.

2. **미루기 금지**
   절대로 일을 미루지 않는다.
   "나중에 하겠습니다", "다음 단계에서 진행하겠습니다", "원하시면 해드릴까요",
   "준비가 되면 알려주세요" 같은 표현을 금지한다.
   지시를 받으면 **그 턴 안에서 끝까지 수행**한다. 설명만 하고 끝내지 않는다.

3. **조사 개수는 하한선**
   50개 / 20개는 최소치다. 미달이면 검색어를 바꿔 추가로 수집한다.
   개수를 채우지 못했다면 임의로 넘어가지 말고 **몇 개를 수집했는지 정확히 보고**한다.
   숫자를 부풀려 보고하지 않는다.

4. **문체**
   모든 블로그 본문은 `~이다` 체로 쓴다.
   `~습니다`, `~해요`, `~하죠`, `~네요`, `~세요`, `~입니다` 금지.

5. **분량**
   본문은 공백 포함 **2000자 이상**. 상한 없음. 길어져도 자르지 않는다.

6. **질문 금지**
   사용자에게 질문하는 지점은 오직 **"주제 3개 중 택1"** 한 번뿐이다.
   그 외에는 질문하지 말고 스스로 판단해서 진행한다.

---

## 프로젝트 목적

부동산 공부를 막 시작한 초보 투자자를 독자로 하는 **입지분석 실전형 블로그 글**을
매주 1편 자동으로 기획·조사·집필·검수한다.

## 디렉토리 구조

```
CLAUDE.md                      ← 이 파일 (절대 규칙)
.claude/agents/topic-scout.md      주제 탐색 서브에이전트
.claude/agents/research-analyst.md 심층 조사 서브에이전트
.claude/agents/writer.md           집필 서브에이전트
.claude/agents/qc-reviewer.md      검수 서브에이전트
.claude/commands/weekly-topics.md  /weekly-topics 커맨드
.claude/commands/weekly-write.md   /weekly-write 커맨드
scripts/count.py               글자수 카운트 + 문체 위반 검출
scripts/notify.sh              OS별 알림 발송
scripts/setup-cron.sh          매주 월요일 09:00 스케줄 등록
research/                      조사 결과 (topics / notes)
posts/                         완성 글
STATUS.md                      진행 상태 기록
```

## 주간 워크플로

```
월요일 09:00 (자동)
   └─ /weekly-topics
        ├─ date 확인
        ├─ topic-scout : 블로그·커뮤니티 글 50개 이상 조사
        ├─ research/YYYY-MM-DD-topics.md 저장
        ├─ 주제 후보 3개 화면 출력
        └─ scripts/notify.sh 알림 발송 → 사용자 번호 응답 대기

사용자가 번호 입력
   └─ /weekly-write [번호]
        ├─ date 확인
        ├─ research-analyst : 관련 글 20개 이상 심층 분석 → notes.md
        ├─ writer           : notes 근거로 집필 → posts/*.md
        ├─ qc-reviewer      : count.py 실행 검수, 미달 시 반려 (최대 3회)
        ├─ 완성 글 전문 채팅 출력 + 파일 경로 안내
        └─ 알림 발송
```

## 글쓰기 원칙

- **독자**: 부동산 공부를 막 시작한 초보 투자자. 용어를 모른다고 가정한다.
- **어려운 용어는 반드시 풀어서 설명한다.** 용어를 던지고 넘어가지 않는다.
- **근거 없는 단정 금지.** notes 파일에 있는 내용만 근거로 쓴다.
  확인되지 않은 수치는 "미확인"으로 표기하고 지어내지 않는다.
- **"이건 몰랐네" 포인트**를 반드시 2~3개 넣는다. 어디서나 볼 수 있는 내용만으로 채우지 않는다.
- 과장·확신 표현("무조건", "반드시 오른다", "100%")을 쓰지 않는다.

## 본문 구조 (writer 고정 템플릿)

1. **도입** — 초보가 실제로 겪는 막막함을 짚으며 시작
2. **핵심 개념** — 용어를 쉬운 말로 풀어서 정의
3. **실전 단계** — 순서대로 따라 할 수 있게 번호로 구성
4. **디테일 한~두 스푼** — 초보 글에는 잘 없는 고수 팁 2~3개 (기준·수치·판별법)
5. **체크리스트** — 현장/실전에서 바로 쓸 수 있는 형태
6. **마무리** — 한 문장 핵심 요약

## 검수 통과 기준

- 공백 포함 2000자 이상
- `~이다` 체 위반 문장 0개
- 설명 없이 던진 전문 용어 0개
- 근거 없는 단정·과장 0개
- 디테일 팁 2개 이상이 실제로 유용
- 내용 중복 없음

## 도구 사용법

```bash
python3 scripts/count.py posts/2026-08-13-example.md   # 글자수 + 문체 검사
bash scripts/notify.sh "제목" "내용"                     # 알림 발송
bash scripts/setup-cron.sh                             # 월요일 09:00 스케줄 등록
bash scripts/setup-cron.sh --remove                    # 스케줄 해제
```
